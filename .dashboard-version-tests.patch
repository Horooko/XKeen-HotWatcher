diff --git a/internal/updater/update_test.go b/internal/updater/update_test.go
index d606285..6de3335 100644
--- a/internal/updater/update_test.go
+++ b/internal/updater/update_test.go
@@ -9,0 +10 @@ import (
+	hw "local/xkeen-hot-watcher/internal/hotwatcher"
@@ -81,0 +83,4 @@ func TestOfflineRepairRecordsBinaryAndPreservesHighWater(t *testing.T) {
+	// Exercise a fixed historical release independently of the current build.
+	oldVersion := hw.Version
+	hw.Version = "0.3.1"
+	defer func() { hw.Version = oldVersion }()
@@ -132 +137 @@ func TestWebUpdatePolicyPreservesMode(t *testing.T) {
-	if err != nil || got.Policy != "minor" || got.Mode != "auto" || !got.Enabled || got.Installed != "0.3.1" {
+	if err != nil || got.Policy != "minor" || got.Mode != "auto" || !got.Enabled || got.Installed != hw.Version {
